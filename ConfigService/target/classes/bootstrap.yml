spring:
  config:
     name: config-server
  cloud:
    config:
      server:
        git:
         uri: https://github.com/ptlsohan/ConfigServerRepository
eureka:
  client:
    registerWithEureka: true    
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8086/eureka/
server:
  port: 8888

  
